# Project configuration (shared across all developers)


# Base project ID for GCP projects
gcp_project_id: str = 'fast-api-app'

# Environments for deployment
environments: list[str] = ["dev", "staging", "prod"]

# Service name for the API
service_name: str = "fast-api"

# Repository name for Artifact Registry
repo_name: str = "fast-api"

# Default region
region: str = "us-central1"

# API configuration
api_port: int = 8000
api_host: str = "0.0.0.0"

# Resource allocation
memory_limit: str = "512Mi"
cpu_limit: str = "1000m"
min_instances: int = 0
max_instances: int = 3
request_concurrency: int = 80
